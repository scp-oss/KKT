# kkt-monitor

Сервис учёта ККТ (контрольно-кассовой техники): реестр касс, загрузка CSV-выгрузок
«Мониторинг ККТ и ФН» и уведомления в Telegram об истечении сроков.

Написан на Go, база — встроенный SQLite (без cgo), весь UI и статика собраны в один
бинарник. Можно запускать в Docker или как обычный исполняемый файл, без контейнера.

## Возможности

- Реестр ККТ (просмотр + поиск) открыт всем без пароля. Пароль (`ADMIN_PASSWORD`)
  нужен только для действий: загрузки CSV, редактирования и удаления записей,
  настроек бота — соответствующие пункты меню и кнопки скрыты, пока не вошли,
  а прямые запросы на эти адреса без входа перенаправляют на страницу логина.
- Любое поле записи можно поправить вручную («Изменить» у строки) — например,
  если исходный CSV обрезан и адрес или другое поле не подтянулись автоматически.
- Загрузка CSV-выгрузки — часть страницы «Настройки» (за паролем). Записи
  уникальны по полю **Заводской номер ККТ** — при повторной загрузке
  существующие обновляются, новые добавляются.
- При загрузке указывается **организация** (например, «ИП Пупкин И.В.») — один
  файл принадлежит одной организации, все ККТ из него помечаются этим названием
  и видны в реестре отдельной колонкой.
- Хранятся поля: Организация, Регистрационный номер ККТ, Заводской номер ФН,
  Заводской номер ККТ, Адрес расчетов, Дата окончания оказания услуг (ОФД),
  Модель ККТ, Дата окончания срока ФН.
- Адрес расчетов при загрузке автоматически приводится к компактному виду —
  из него убираются код региона, почтовый индекс, название области/района и
  прочий канцелярский мусор, остаётся «г Город, ул Улица, д. N».
- Реестр ККТ отсортирован по срочности: записи, у которых раньше всего истекает
  срок услуг ОФД **или** срок ФН (берётся более ранняя из двух дат), — сверху;
  записи без истекающих сроков — снизу. Строки с приближающимся сроком подсвечены.
- Быстрый поиск по реестру — по названию организации и по адресу установки.
- Уведомления в Telegram за **30, 10, 5, 2 и 1 день** до окончания срока — отдельно
  по дате окончания услуг ОФД и по дате окончания срока ФН. Проверка идёт по
  расписанию — одно или два времени в сутки (настраивается), время сервера.
- Несколько получателей уведомлений (по chat_id), каждого можно включить/отключить.
  Получателя можно привязать к конкретной организации — тогда он видит уведомления
  только по её ККТ; получатель без привязки («Все организации») получает всё.
- Бот может слать сообщения тремя способами (переключаются радиокнопкой,
  взаимоисключающе): напрямую в `api.telegram.org`, через SOCKS5-прокси, либо
  через свой relay/зеркало (например,
  `https://your-domain.tld/bot{TOKEN}/sendMessage?auth={AUTH_KEY}`).
- Настройки бота и загрузка CSV доступны только администратору; секретные
  значения (токен, auth_key, адрес relay, адрес SOCKS5) не отображаются повторно
  в форме — сохраняются только через сервер, поле остаётся пустым до перезаписи.
  Настройки бота читаются заново при каждой отправке — изменили и сохранили,
  перезапуск сервиса не нужен.

## Быстрый старт без Docker

Нужен Go 1.25+.

```bash
cp .env.example .env
# отредактируйте .env — задайте ADMIN_PASSWORD

export $(grep -v '^#' .env | xargs)
go run .
```

Сервис поднимется на `http://localhost:8080`. База SQLite создастся автоматически
по пути `DB_PATH` (по умолчанию `data/kkt.db`).

Сборка отдельного бинарника:

```bash
go build -o kkt-monitor .
ADMIN_PASSWORD=... ./kkt-monitor
```

## Запуск в Docker

```bash
cp .env.example .env
# отредактируйте .env

docker compose up -d --build
```

Данные (SQLite) сохраняются в volume `kkt-data`, переживают пересборку контейнера.

Либо без compose:

```bash
docker build -t kkt-monitor .
docker run -d --name kkt-monitor \
  -p 8080:8080 \
  -e ADMIN_PASSWORD=change-me \
  -v kkt-data:/data \
  kkt-monitor
```

## Переменные окружения

| Переменная      | По умолчанию   | Описание                                              |
|-----------------|----------------|--------------------------------------------------------|
| `ADMIN_PASSWORD`| —              | Обязателен. Пароль для действий: загрузка CSV, удаление записей, настройки бота/получателей. Сам реестр (просмотр + поиск) открыт без пароля. |
| `PORT`          | `8080`         | Порт, на котором слушает сервис. Задайте свой (например, `PORT=9090`) и пробросьте тот же порт в Docker (`-p 9090:9090` / `${PORT}:${PORT}` в compose). |
| `LISTEN_ADDR`   | —              | Необязательная альтернатива `PORT`: полный адрес вида `host:port`, если нужно слушать конкретный интерфейс. Если задан, имеет приоритет над `PORT`. |
| `DB_PATH`       | `data/kkt.db`  | Путь к файлу базы SQLite.                               |
| `COOKIE_SECURE` | `false`        | Флаг Secure для cookie сессии. Включайте (`true`) только когда сервис реально работает по HTTPS — иначе браузер молча отбрасывает cookie сессии, и после входа вас будет постоянно возвращать на страницу пароля. |

Порт в Docker не зашит намертво: он берётся из `PORT` (по умолчанию 8080) и
пробрасывается наружу тем же значением — достаточно задать `PORT` в `.env` и
пересобрать/перезапустить контейнер, чтобы сервис слушал другой порт.

## Настройка Telegram-бота

Все настройки бота задаются в веб-интерфейсе (`/settings`), а не через переменные
окружения — чтобы адрес relay/токен нигде не «светился» в открытом виде.

1. Создайте бота через [@BotFather](https://t.me/BotFather), получите токен.
2. В `/settings` задайте расписание уведомлений (одно или два времени ЧЧ:ММ в
   сутки, время сервера) и вставьте токен бота.
3. Выберите способ отправки:
   - **Напрямую** — сообщения идут прямо в `api.telegram.org`.
   - **Через SOCKS5** — укажите адрес прокси, например
     `socks5://user:pass@host:1080`.
   - **Через relay-сервер** — укажите только домен зеркала Bot API, например
     `example.tld` (или `https://example.tld`), и, если нужно, ключ
     авторизации. Путь запроса (`/bot{TOKEN}/sendMessage?auth={ключ}`) сервис
     подставляет сам — эту часть вводить не нужно.
4. Добавьте получателей — их Telegram chat_id (узнать свой chat_id можно у бота
   [@userinfobot](https://t.me/userinfobot)). При желании привяжите получателя
   к конкретной организации, чтобы он видел уведомления только по её ККТ.
5. Нажмите «Отправить тестовое сообщение всем», чтобы проверить настройки.

Уведомления рассылаются автоматически: фоновый планировщик проверяет реестр по
расписанию и отправляет сообщение подходящим получателям (с учётом привязки к
организации) ровно один раз при достижении порогов 30, 10, 5, 2 и 1 день до
`Дата окончания оказания услуг` и до `Дата окончания срока ФН`. Повторная
загрузка CSV с новой датой (например, после продления) снова включает отсчёт
порогов для этой записи.

## Формат CSV

Ожидается выгрузка «Мониторинг ККТ и ФН» — разделитель `;`, значения ячеек в
формате `="значение"`, кодировка UTF-8 (с BOM или без). Колонки определяются по
заголовку, поэтому порядок столбцов в файле не важен — важны только их названия.
